# 📚 Library Management System Using C++

A simple console-based **C++ Library Management System** that manages book records using **file handling (`fstream`)** for persistent storage.

---

## 🚀 Features

The system supports the following functionalities via a main menu:

- **Show All Books** → Lists all book records stored in the system.  
- **Extract Book by ID** → Searches and displays details of a single book using its unique ID.  
- **Add Book** → Adds a new book record with *Book ID*, *Book Name*, and *Author Name*.  
- **Exit** → Exits the application safely.

---

## ⚙️ Setup and Run

### 🧩 Prerequisites

- A C++ compiler (e.g., `g++`)
- A terminal or command prompt

### 🛠️ Build and Execute

1. **Save** the code as `library_management.cpp`
2. **Compile** using:

   ```bash
   g++ library_management.cpp -o library_app
   
3. **Run this Application**
   ```bash
   ./library_app
